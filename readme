Make sure you add .env file in backend

MongoDB Password
JWTSecret Token

MONGO_URI=<Your URL>
JWT_SECRET=<Your Token>

JWT Token can be created but mongodb token needs to be attained Make sure you add own mongo DB URl